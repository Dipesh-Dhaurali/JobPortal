# ─────────────────────────────────────────────
#  Job Portal – Python Dependencies
# ─────────────────────────────────────────────

# Core Web Framework
Django==5.1.7

# Image Processing (used for profile/resume photo uploads via ImageField)
Pillow==10.4.0

# Machine Learning – Recommendation System
# (CountVectorizer + cosine_similarity in candidate/recommended.py)
scikit-learn==1.6.1

# Natural Language Processing – Stopwords for CountVectorizer
nltk==3.9.1

# Numerical Computing (required by scikit-learn / .toarray())
numpy==2.2.3

# ─────────────────────────────────────────────
#  Optional / Production Dependencies
# ─────────────────────────────────────────────

# WSGI server for production deployment (e.g. on Heroku/VPS)
gunicorn==21.2.0

# Serve static files efficiently in production (replaces WhiteNoise/nginx workaround)
whitenoise==6.8.2

# Environment variable management (recommended for hiding SECRET_KEY & email password)
# python-decouple==3.8
